# PDF Inspector 产品需求规格说明（SPEC）

> 文档状态：Draft  
> 版本：0.1  
> 基线日期：2026-09-01  
> 适用产品版本：0.1.x  
> 后续产物：软件设计说明（SDD）

## 1. 文档目的

本文档定义 PDF Inspector Chrome 扩展的产品边界、功能需求、非功能需求、外部接口与验收标准，为后续 SDD、开发、测试和发布提供统一输入。

本文档以仓库当前代码为事实基线。标记为“基线”的条目描述当前版本必须保留的能力；标记为“候选”的条目仅用于路线图讨论，不属于当前版本的验收范围。

## 2. 产品概述

PDF Inspector 是一个在 Chrome 浏览器扩展弹窗中运行的 PDF 转 Markdown 工具。用户可选择本地 PDF，或输入/自动识别网络 PDF URL。扩展在本机通过 Web Worker 和 WASM 完成解析，展示 Markdown 结果，并允许复制或下载。

### 2.1 产品目标

- 让用户以尽可能少的操作将单个 PDF 转换为可编辑的 Markdown。
- PDF 内容解析在本地完成，不将文档上传至产品自有服务器。
- 解析期间保持弹窗界面可响应，并向用户提供明确的进度或错误反馈。
- 为复制、归档及后续内容处理提供便捷的结果导出方式。

### 2.2 成功标准

- 对受支持且包含可提取文本的 PDF，可生成非空 Markdown。
- 本地文件与 HTTP(S) URL 两条输入链路均可完成解析。
- 用户可在一次弹窗会话内完成“输入—解析—查看—复制/下载”。
- 失败不会导致弹窗崩溃，且界面会显示可理解的错误信息。

### 2.3 范围外

当前基线不包含：

- 扫描件或图片型 PDF 的 OCR。
- 多文件批处理。
- 页码范围选择。
- HTML、JSON 等 Markdown 之外的输出格式。
- 解析历史、账户、云同步或云存储。
- Markdown 预览、所见即所得编辑或结果持久化。
- 对加密、受密码保护或损坏 PDF 的恢复处理。

## 3. 用户与运行环境

### 3.1 目标用户

- 需要提取论文、报告、合同等 PDF 文本的知识工作者。
- 需要将 PDF 内容转换为 Markdown 供笔记、搜索或后续自动化处理的用户。

### 3.2 运行环境

- Chromium/Chrome 浏览器，支持 Manifest V3、Web Worker 和 WebAssembly。
- 扩展以 800 × 900 px 的弹窗为主要交互界面。
- 网络 URL 输入依赖目标服务器可访问，且允许扩展发起跨域请求。

### 3.3 外部依赖

- Chrome Extension APIs：活动标签页查询及扩展运行环境。
- 浏览器 Fetch、Clipboard、Blob 和下载能力。
- `@firecrawl/pdf-inspector-wasm`：PDF 分类与 Markdown 转换。
- Vite：构建与 Worker/WASM 资源打包。

## 4. 核心用户流程

### UC-01 本地 PDF 转换

1. 用户打开扩展，默认进入“本地文件”页签。
2. 用户选择一个 PDF 文件。
3. 系统读取文件并进入解析状态。
4. Worker 调用 WASM 转换文件。
5. 系统显示 PDF 类型与 Markdown；失败时显示错误。
6. 用户复制结果或下载 `.md` 文件。

### UC-02 URL PDF 转换

1. 用户进入“URL 链接”页签并输入 URL。
2. 用户点击“加载 PDF”或按 Enter。
3. 系统校验 URL、下载资源并验证响应。
4. 系统解析并展示结果，后续操作同 UC-01。

### UC-03 当前标签页 PDF 自动识别

1. 用户在直接 PDF 页面或 Chrome 内置 Adobe PDF Viewer 页面打开扩展。
2. 系统读取当前活动标签页 URL。
3. 若识别为 PDF，系统切换到 URL 页签并自动填入原始 PDF URL。
4. 用户确认后手动触发加载。

## 5. 功能需求

优先级定义：P0 为核心能力，P1 为重要体验，P2 为增强项。

### 5.1 输入与导航

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-001 | P0 | 系统应提供“本地文件”和“URL 链接”两个互斥页签。 |
| FR-002 | P0 | 弹窗首次打开时应默认显示“本地文件”页签。 |
| FR-003 | P0 | 用户切换页签时，系统应同步更新选中样式与可见输入区。 |
| FR-004 | P0 | 本地文件选择器应声明仅接受 `application/pdf`。 |
| FR-005 | P0 | 系统应拒绝未选择文件或浏览器识别 MIME 类型不是 `application/pdf` 的文件，并显示“请选择 PDF 文件”。 |
| FR-006 | P0 | URL 输入应接受用户输入的绝对 URL；空值或无法由 URL 解析器解析的值应被拒绝。 |
| FR-007 | P1 | URL 输入框按 Enter 应等价于点击“加载 PDF”。 |

### 5.2 当前页面自动识别

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-010 | P1 | 弹窗初始化时，系统应查询当前窗口的活动标签页。 |
| FR-011 | P1 | 对路径以 `.pdf` 结尾并可带查询参数的直接 URL，系统应识别为 PDF URL。 |
| FR-012 | P1 | 对形如 `chrome-extension://<viewer-id>/<encoded-original-url>` 的 PDF Viewer URL，系统应提取并解码原始 URL。 |
| FR-013 | P1 | 识别成功后，系统应切换到 URL 页签、填入 URL，并显示“已检测到 PDF 链接”。 |
| FR-014 | P1 | 自动识别只应预填输入，不应在未经用户确认时自动下载或解析远程资源。 |
| FR-015 | P1 | 标签页不可访问或无法识别时，不应阻断其他功能。 |

### 5.3 URL 下载与校验

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-020 | P0 | 系统应通过 Fetch 下载用户指定的 HTTP(S) PDF。 |
| FR-021 | P0 | 下载开始后，系统应显示“正在下载…”，并禁用复制和下载操作。 |
| FR-022 | P0 | 非 2xx 响应应视为失败，并在错误信息中包含 HTTP 状态。 |
| FR-023 | P0 | 响应 `Content-Type` 缺失或不包含 `application/pdf` 时，系统应拒绝处理。 |
| FR-024 | P0 | 下载成功后，系统应将响应体读取为 `ArrayBuffer` 并交由统一解析流程。 |
| FR-025 | P1 | 输出文件基名应优先取 URL 路径最后一段；无法取得时使用 `document`。 |
| FR-026 | P0 | 当前版本只支持 `http:` 与 `https:` URL；其他协议应被拒绝。 |

### 5.4 PDF 解析

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-030 | P0 | PDF 解析应在模块型 Web Worker 中执行，避免在 UI 主线程进行主要解析工作。 |
| FR-031 | P0 | 系统应使用 Transferable `ArrayBuffer` 将 PDF 数据传给 Worker，避免不必要的完整内存复制。 |
| FR-032 | P0 | Worker 应在首次解析前初始化 WASM，并在后续请求中复用初始化结果。 |
| FR-033 | P0 | Worker 应将输入转换为 `Uint8Array` 后调用 PDF 处理器。 |
| FR-034 | P0 | 处理配置应使用 `profile: "compact"` 且启用页面标记。 |
| FR-035 | P0 | 解析期间，系统应清空旧结果、显示“正在分析…”，并禁用复制与下载。 |
| FR-036 | P0 | 成功响应应包含 PDF 类型和 Markdown；界面应显示成功状态及 PDF 类型。 |
| FR-037 | P0 | Markdown 为空或缺失时，界面应提示该 PDF 可能需要 OCR。 |
| FR-038 | P0 | Worker 捕获到异常时，应返回可序列化的错误字符串，界面显示处理失败。 |
| FR-039 | P1 | Worker 应兼容 `{buffer, fileName}` 消息；是否继续兼容直接 `ArrayBuffer` 由 SDD 明确。 |

### 5.5 结果展示与导出

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-040 | P0 | 系统应在可编辑的多行文本区域显示 Markdown 结果。 |
| FR-041 | P0 | 解析成功后，应启用复制和下载操作。 |
| FR-042 | P0 | 点击复制时，系统应将当前完整结果写入剪贴板。 |
| FR-043 | P1 | 复制成功后，按钮应短暂显示成功反馈，并在约 2 秒后恢复。 |
| FR-044 | P0 | 复制失败时，系统应显示错误状态且不丢失结果。 |
| FR-045 | P0 | 点击下载时，系统应生成 UTF-8 Markdown Blob 并下载为 `<输入文件基名>.md`。 |
| FR-046 | P0 | 本地文件名的 `.pdf` 后缀应以不区分大小写方式移除。 |
| FR-047 | P1 | 导出后应释放临时 Object URL。 |
| FR-048 | P0 | 无结果时，复制和下载动作不得产生空导出。 |

### 5.6 状态与错误反馈

系统至少应支持下列 UI 状态：

| 状态 | 进入条件 | 允许的主要操作 | 退出条件 |
|---|---|---|---|
| 初始 | 弹窗打开且未识别 PDF | 选择文件、输入 URL | 用户输入或自动识别 |
| 已识别 | 当前标签识别出 PDF | 编辑 URL、加载 | 用户加载或切换输入 |
| 下载中 | URL 请求已发起 | 等待 | 下载成功或失败 |
| 解析中 | PDF 已交给 Worker | 等待 | Worker 成功或失败 |
| 成功 | 获得 Worker 成功响应 | 编辑、复制、下载、新解析 | 用户发起新解析 |
| 错误 | 校验、下载、解析或复制失败 | 修改输入、重试 | 用户发起后续操作 |

| ID | 优先级 | 需求 |
|---|---:|---|
| FR-050 | P0 | 信息、处理中、成功和错误状态应有可区分的文字与视觉样式。 |
| FR-051 | P0 | 每次新解析开始时应使上一份结果不可继续导出。 |
| FR-052 | P1 | 用户可见错误应说明失败阶段（校验、下载、处理或复制）。 |
| FR-053 | P1 | 内部异常不得以未捕获异常形式终止主要交互流程。 |

## 6. 外部接口与数据契约

### 6.1 Popup → Worker

规范消息：

```ts
type ProcessPdfRequest = {
  buffer: ArrayBuffer;
  fileName: string;
};
```

约束：

- `buffer` 必须包含完整 PDF 二进制内容。
- `fileName` 用于会话语义和可观测性；当前 Worker 不依赖该字段生成结果。
- `buffer` 应通过 transfer list 转移所有权。

### 6.2 Worker → Popup

```ts
type ProcessPdfResponse =
  | {
      ok: true;
      result: {
        pdfType: string;
        markdown?: string;
      };
    }
  | {
      ok: false;
      error: string;
    };
```

SDD 应进一步定义 `pdfType` 的允许值、Markdown 页面标记格式，以及依赖版本变化时的兼容策略。

### 6.3 网络接口

- 请求方法：GET。
- 允许协议：HTTP、HTTPS。
- 成功条件：`response.ok === true`。
- 内容条件：响应头 `Content-Type` 包含 `application/pdf`。
- 身份认证：当前版本不主动注入凭据或认证头。
- 重定向、超时和最大文件尺寸：当前未显式定义，列为待确认项。

## 7. 非功能需求

### 7.1 性能与资源

| ID | 需求 |
|---|---|
| NFR-001 | 主要 PDF 解析不得阻塞 UI 主线程。 |
| NFR-002 | UI 在下载和解析期间应能继续绘制并响应基本交互。 |
| NFR-003 | WASM 在同一弹窗 Worker 生命周期内最多初始化一次。 |
| NFR-004 | 二进制数据传递应优先使用 Transferable，解析完成后应释放临时下载 URL。 |
| NFR-005 | 最大支持文件大小、下载超时和目标处理时长须在 SDD 或性能基准阶段量化。 |

### 7.2 安全与隐私

| ID | 需求 |
|---|---|
| NFR-010 | PDF 解析应在用户本地浏览器执行，不上传至产品自有后端。 |
| NFR-011 | 扩展不得采集遥测、文档内容或用户身份信息，除非未来版本另行取得明确同意。 |
| NFR-012 | 远程加载只允许 HTTP(S)，不得处理 `file:`、`javascript:`、`data:` 等协议。 |
| NFR-013 | 扩展 CSP 应仅允许自身脚本，并只为 WASM 启用所需能力。 |
| NFR-014 | 浏览器权限与 host permissions 应遵循最小权限原则；广泛 HTTP(S) 主机权限的必要性须在 SDD 中记录。 |
| NFR-015 | 错误信息不得包含 PDF 正文或其他非必要敏感内容。 |

### 7.3 可靠性与兼容性

| ID | 需求 |
|---|---|
| NFR-020 | 下载、WASM 初始化和 PDF 处理异常均应转换为用户可见错误。 |
| NFR-021 | 单次失败后，用户无需重新安装或刷新扩展即可选择其他输入重试。 |
| NFR-022 | 构建产物应符合 Chrome Manifest V3 要求并可作为未打包扩展加载。 |
| NFR-023 | 依赖升级不得在未验证消息契约与 Markdown 输出兼容性的情况下进入发布版本。 |

### 7.4 易用性与可访问性

| ID | 需求 |
|---|---|
| NFR-030 | 核心操作和状态应使用简体中文，产品名与通用格式名可保留英文。 |
| NFR-031 | 状态不得仅依赖颜色表达，必须同时提供文字。 |
| NFR-032 | URL 加载应可通过键盘 Enter 触发；其余交互控件应可通过键盘聚焦和激活。 |
| NFR-033 | 文本与背景、焦点状态和禁用状态应满足目标可访问性等级；目标等级待确认，建议 WCAG 2.1 AA。 |
| NFR-034 | 固定弹窗尺寸下，核心输入、状态、结果和导出操作应可见且不发生横向溢出。 |

### 7.5 可维护性与可测试性

| ID | 需求 |
|---|---|
| NFR-040 | UI 编排、PDF 处理和样式应保持模块边界，解析依赖仅在 Worker 内调用。 |
| NFR-041 | 功能需求应能通过唯一 ID 追踪到 SDD 组件、实现和测试用例。 |
| NFR-042 | 发布构建应可通过单一标准命令生成，且构建失败返回非零退出码。 |

## 8. 验收标准

### AC-01 本地有效 PDF

- Given 用户选择 MIME 为 `application/pdf` 的文本型 PDF
- When 文件读取及 Worker 处理成功
- Then 状态显示成功及 PDF 类型，结果区显示 Markdown，复制与下载可用

### AC-02 本地无效文件

- Given 用户未选择文件或选择非 PDF MIME 文件
- When 触发文件变更
- Then 系统显示“请选择 PDF 文件”，且不发起解析

### AC-03 有效远程 PDF

- Given 用户输入可访问的 HTTP(S) URL，响应成功且 Content-Type 为 PDF
- When 点击加载或按 Enter
- Then 系统依次进入下载中和解析中，最终展示解析结果

### AC-04 非 PDF 远程资源

- Given URL 返回成功，但 Content-Type 不是 PDF 或缺失
- When 下载响应到达
- Then 系统显示下载失败及“返回的内容不是 PDF 文件”，不调用解析

### AC-05 HTTP 失败

- Given URL 返回非成功状态
- When 下载响应到达
- Then 错误信息包含 HTTP 状态码，复制与下载不可用

### AC-06 自动识别

- Given 当前标签页为直接 PDF URL 或支持的 Viewer 包装 URL
- When 用户打开弹窗
- Then 系统切到 URL 页签、填入原始 URL、显示检测状态，但不自动请求资源

### AC-07 无可提取文本

- Given Worker 成功返回但 Markdown 为空
- When Popup 渲染结果
- Then 结果区提示可能需要 OCR，用户仍可复制或下载该提示

### AC-08 复制结果

- Given 已成功生成非空结果
- When 用户点击复制
- Then 剪贴板内容与结果区完整一致，按钮短暂显示“已复制”

### AC-09 下载结果

- Given 输入文件为 `Quarterly.Report.PDF` 且已成功生成结果
- When 用户点击下载
- Then 浏览器发起名为 `Quarterly.Report.md` 的 Markdown 文件下载

### AC-10 失败后重试

- Given 上一次下载或解析失败
- When 用户提供另一个有效 PDF
- Then 系统可进入新的解析流程并成功展示结果

## 9. 需求追踪矩阵

| 用户流程 | 主要功能需求 | 主要非功能需求 |
|---|---|---|
| UC-01 本地转换 | FR-001～005、FR-030～039、FR-040～048 | NFR-001～004、NFR-010～015、NFR-020～023 |
| UC-02 URL 转换 | FR-006～007、FR-020～026、FR-030～048 | NFR-001～005、NFR-010～015、NFR-020～023 |
| UC-03 自动识别 | FR-010～015 | NFR-014、NFR-020、NFR-030～034 |
| 状态与恢复 | FR-050～053 | NFR-020～023、NFR-031 |

后续 SDD 应为每个 P0/P1 需求补充“设计组件/接口/测试用例”三列，形成双向追踪。

## 10. 候选路线图（不纳入当前验收）

| ID | 候选能力 | 进入正式范围前必须明确 |
|---|---|---|
| CAND-001 | OCR | OCR 引擎、语言、隐私、包体积、性能与是否允许云端处理 |
| CAND-002 | 批处理 | 并发数、队列、取消、聚合导出与内存限制 |
| CAND-003 | 页码范围 | 页面索引规则、解析器支持与 UI 交互 |
| CAND-004 | HTML/JSON 输出 | 格式 schema、兼容性与文件命名 |
| CAND-005 | 处理历史 | 存储位置、容量、保留期、清除和隐私策略 |
| CAND-006 | 云集成 | 服务商、认证、权限、数据驻留与失败补偿 |

## 11. 假设、差异与待确认项

### 11.1 当前假设

- 产品以单用户、单弹窗、单次处理一个 PDF 为基线。
- 远程服务器会提供正确的 PDF Content-Type；不基于文件签名或内容嗅探兜底。
- WASM 返回结构至少包含 `pdfType`，并可能包含 `markdown`。
- 用户关闭弹窗即结束当前会话，结果不持久化。

### 11.2 代码与规格差异

以下条目是从当前代码识别出的规范化要求，后续实现或 SDD 需要处理：

- FR-026 要求仅允许 HTTP(S)，当前 URL 校验接受任何可被 `new URL()` 解析的协议。
- 初始 HTML 未声明复制和下载按钮为 disabled；逻辑虽会阻止空输出，但视觉状态与 FR-048 尚不完全一致。
- Manifest 声明了 `downloads` 权限，但当前下载通过临时 `<a>` 元素触发，并未调用 `chrome.downloads` API。
- README 声称支持拖放上传，当前 UI/脚本未实现拖放事件；本 SPEC 不将其纳入基线。
- README 声称“无滚动条”，样式实际允许主内容纵向滚动；本 SPEC 只禁止横向溢出。
- README 与 AGENT.md 引用了不存在的 `SDD.md`；应在本 SPEC 评审通过后生成。

### 11.3 SDD 前待确认

1. 目标 Chrome 最低版本及是否支持其他 Chromium 浏览器。
2. 最大 PDF 文件大小、下载超时、解析超时与内存上限。
3. URL 是否允许携带查询参数、基本认证信息或需要 Cookie 的受保护资源。
4. `Content-Type` 不规范但文件签名有效时是否允许继续处理。
5. 是否需要取消正在进行的下载/解析，以及连续提交时的竞态处理规则。
6. `pdfType` 的完整枚举及面向用户的中文映射。
7. 空 Markdown 时是否应允许导出提示文本，还是应作为“无结果”错误处理。
8. 结果区是否有意允许用户编辑；若允许，复制/下载应导出编辑后的内容。
9. 是否保留 Worker 对直接 `ArrayBuffer` 旧消息格式的兼容。
10. 是否删除未使用的 `downloads` 权限，或改用 `chrome.downloads` API。

## 12. SDD 输入清单

在本 SPEC 获得评审确认后，SDD 至少应覆盖：

- 系统上下文、运行时组件和信任边界。
- Popup 状态机、事件模型及并发/竞态处理。
- Popup 与 Worker 消息协议、错误分类和版本兼容策略。
- WASM 初始化、资源加载、内存生命周期和性能预算。
- URL 下载策略、协议白名单、超时、大小限制及 Content-Type/文件签名校验。
- Manifest 权限、CSP、隐私模型和威胁分析。
- 文件命名、字符编码、Markdown 页面标记与 `pdfType` 数据字典。
- 单元、集成、端到端、性能与安全测试方案，以及 FR/NFR 追踪矩阵。
- 构建、打包、版本、发布和回滚方案。

